# Firestore Repositories

Concrete Firestore repository implementations belong here. Implementations
must satisfy repository interfaces declared by the domain layer and must not
expose Firebase document types outside the infrastructure boundary.
